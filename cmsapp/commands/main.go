package commands

const (
	ServeHelp   = "[or s] run server on port"
	DBBuildHelp = "[or dbb] create all tables"
	DBLoadHelp  = "[or dbl] load all sql file from path (--path=database/fixtures)"

	// FixtureDir is default path to fixtures directory
	DefaultFixtureDir = "database/fixtures"
)
