{{- define "sqlite.sqltype" -}}

{{- if eq (lower .) "text" "string" -}}
  TEXT
{{- else if eq (lower .) "int" -}}
  INTEGER
{{- else -}}
  {{- error (print "bad SQL type" .) -}}
{{- end -}}

{{- end -}}
